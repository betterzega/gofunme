<nav class="navbar navbar-expand-lg navbar-dark bg-primary navbar-fixed-top">
  <a class="navbar-brand" href="/">GoFunMe</a>
  <ul class="navbar-nav mr-auto">
    <li class="nav-item <%= "active" if active == "discover" %>">
      <%= link_to "Discover", events_path, class: 'nav-link' %>
    </li>

    <% if user_signed_in? %>
      <li class="nav-item <%= "active" if active == "my_events" %>">
        <%= link_to "My events", user_events_path(current_user), class: 'nav-link' %>
      </li>
    <% end %>
  </ul>
  <ul class="navbar-nav ml-auto">
    <% if user_signed_in? %>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Hello, <%= current_user.first_name %>!          </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="#">Account</a>
          <%= link_to "Log out", destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
        </div>
      </li>
    <% else %>
      <li class="nav-item">
        <%= link_to "Sign in", new_user_session_path, class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <%= link_to "Sign up", new_user_registration_path, class: 'nav-link' %>
      </li>
    <% end %>
  </ul>
</nav>
